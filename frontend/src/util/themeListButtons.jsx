import { createTheme } from "@mui/material/styles";

// Create themes for list buttons on practice page
const themeListButtons = createTheme({
  palette: {
    primary: { main: "#ffffff" },
    secondary: { main: "#000000" },
  },
});

export default themeListButtons;
