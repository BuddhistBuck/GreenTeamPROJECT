import { createTheme } from "@mui/material/styles";

// Create themes for user button components on practice page
const themeUserButtons = createTheme({
  palette: {
    primary: { main: "#4f7fa1" },
    secondary: { main: "#a9cbd6" },
  },
});

export default themeUserButtons;
